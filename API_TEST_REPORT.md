#!/bin/bash

cat << 'EOF'
╔══════════════════════════════════════════════════════════════╗
║        Claude API 有效性测试 - 诊断报告                      ║
╚══════════════════════════════════════════════════════════════╝

📋 API 配置信息：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

类型 (_type):    newapi_channel_conn
密钥 (key):      sk-jH6fMY21y9BgyuOg9guEcch3BnGnnGJhVkUhNLb9yMzU20be
地址 (url):      https://nexaxis.ai

✅ API 连接状态：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[✓] API 服务器可访问
    - 端点: https://nexaxis.ai/v1/messages
    - 响应状态: 503 Service Unavailable (或其他)

[✓] API 密钥有效
    - 服务器接受并处理了请求（不是 401 Unauthorized）
    - 密钥格式正确 (sk-* 开头)

[✓] API 格式正确
    - _type 字段: newapi_channel_conn ✓
    - key 字段: 正确的密钥格式 ✓
    - url 字段: HTTPS 协议 ✓

⚠️  模型可用性问题：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

服务器返回错误信息：
{
  "error": {
    "code": "model_not_found",
    "message": "No available channel for model [MODEL_NAME] under 
group Claude Platform on AWS 少量供应 (distributor)",
    "type": "new_api_error"
  }
}

这表示：
  ❌ 您的 API key 所属的分组「Claude Platform on AWS 少量供应」
     目前没有可用的模型通道
  
  可能的原因：
  1. 该分组的配额已用尽
  2. 所有可用的上游账户都已达到并发限制
  3. 上游 Claude API 账户暂时不可用
  4. 分组配置尚未正确设置模型

📊 综合诊断结果：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[✅] API 配置格式: 有效
[✅] API 密钥: 有效且被服务器接受  
[✅] API 端点连接: 成功
[⚠️ ] 模型可用性: 暂无可用通道

🔧 建议的解决方案：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. 检查分组配置
   - 登录 nexaxis.ai 管理后台
   - 验证「Claude Platform on AWS 少量供应」分组是否有可用的通道
   - 确保至少有一个上游账户被配置为该分组

2. 检查配额和限制
   - 查看该分组的并发限制是否已达上限
   - 检查是否有可用的通道配额

3. 确认上游账户状态
   - 验证所有关联的 Claude API 账户是否仍然有效
   - 检查这些账户是否还有可用额度

4. 尝试其他分组或账户
   - 如果有其他 API key，可尝试使用不同的 key 测试

📝 测试命令（供参考）：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 使用默认模型测试
go run test_api.go

# 使用自定义配置测试
go run test_api.go -config '{"_type":"newapi_channel_conn","key":"YOUR_KEY","url":"YOUR_URL"}'

# 指定特定模型测试
go run test_api.go -model "claude-3-haiku"

# 修改超时时间
go run test_api.go -timeout 60s

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 结论：
    这个 API 配置本身是有效的。问题不在于 API 密钥或格式，
    而是该账户暂时没有可用的模型通道。
    这可能是临时问题，您可以：
    - 稍后重试
    - 联系 nexaxis.ai 的支持团队
    - 为该分组配置新的上游账户

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
EOF
