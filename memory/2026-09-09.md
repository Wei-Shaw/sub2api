## 12:28 [GPT-5]
- 完成：在原版/简约主题切换后新增独立的自定义背景入口，支持选择图片、调整透明度、应用或清除背景，并通过本地存储持久化。
- 决策：自定义背景使用独立的 `custom-background` 配置，不改变 `ui-theme`，以满足自定义只修改背景配置的要求。

## 12:42 [GPT-5]
- 完成：重新构建并更新本地 `sub2api-dev` Docker 容器，容器健康检查通过且本地网页返回 HTTP 200。
- 决策：仅重建并替换应用容器，保持现有 PostgreSQL 和 Redis 容器继续运行。

## 13:20 [GPT-5]
- 完成：排查自定义背景未显示问题，改为使用浏览器本地 IndexedDB 保存图片 Blob 和 Blob URL 渲染，移除固定图片大小限制，并让自定义背景覆盖默认渐变与网格背景。
- 决策：图片不经过 Sub2API 后端；保留旧版 `localStorage` 数据的兼容迁移，避免已有背景丢失。

## 13:37 [GPT-5]
- 完成：为自定义背景弹窗增加“组件效果”配置，支持磨砂玻璃和组件透明，并将效果持久化到浏览器本地存储；完成前端检查、测试、构建及 Docker 容器更新。
- 决策：组件效果仅在启用自定义背景时覆盖通用页面组件的背景与边框样式，原版和简约主题配置保持独立。

## 13:49 [GPT-5]
- 完成：为自定义背景增加独立的开启/关闭开关，关闭时保留本地图片、透明度和组件效果配置，仅隐藏背景并停用组件效果；完成测试、构建和本地 Docker 更新。
- 决策：只有“清除背景”操作才删除图片配置，开启状态使用独立字段持久化并兼容旧版配置。

## 14:18 [GPT-5]
- 完成：从远程仓库 `custom-theme` 分支的 `fbfecc1a5` 构建并部署美国-69 前端容器，应用健康检查、公网健康接口和首页访问均通过。
- 决策：仅替换 `sub2api` 应用容器；保留 PostgreSQL、Redis、数据挂载目录及旧镜像回退标签，并在切换前创建应用数据、Redis 和 PostgreSQL 备份。

## 15:26 [GPT-5]
- 完成：同步上游 `main` 的 `v0.2.4` 更新，合并到 `custom-theme`，保留主题与自定义背景改动，并推送 fork 远端。
- 决策：采用无冲突合并提交 `04b2e08b6`；主题相关测试和前端生产构建均通过。

## 15:35 [GPT-5]
- 完成：基于 `custom-theme` 合并后的源码重建本地 Docker 镜像并更新 `sub2api-dev` 应用容器，保留 PostgreSQL、Redis 及持久化数据。
- 决策：使用 `deploy/docker-compose.dev.yml` 执行本地源码构建部署；应用、PostgreSQL 和 Redis 健康检查均通过。

## 17:58 [GPT-5]
- 完成：将 `custom-theme` 合并提交 `04b2e08b6` 部署到美国-69（38.65.94.106）远端 Docker，应用容器健康运行，公网域名与健康接口均返回 200。
- 决策：仅替换 `sub2api` 应用容器；保留 PostgreSQL、Redis、数据挂载目录，并创建 `pre-frontend-20260909-095513` 数据库、缓存和应用数据备份及回退镜像。
