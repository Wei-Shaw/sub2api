## 00:36 [GPT-5]
- 完成：普通用户账号管理列表恢复操作列并补齐列设置、自动刷新、排序记忆和分组选项卡拖动排序，操作菜单仅保留连接测试与查看统计，同时完成可见关系鉴权、专项测试和本地 Docker 重建。
- 决策：普通用户列表继续使用独立的账号可见关系查询；移除管理员详情接口复用，避免向普通用户返回编辑所需的完整账号数据；所有诊断接口均先校验账号与当前用户的可见关系。

## 00:53 [GPT-5]
- 完成：将账号可见管理功能提交并推送至 origin/custom-theme，生成包含 Docker 镜像、增量 SQL 和校验文件的部署包，并完整传输到美国-69服务器。
- 决策：按用户最新要求仅上传并校验部署包，不导入镜像、不执行 SQL、不重启远端服务。
- 待办：需要更新时，在远端使用 releases/73bcdf216 目录中的部署包执行升级。
