// Code generated by gen-frontend-constants; DO NOT EDIT.
//
// Source: plugins/channel-management/service/channel.go
// Run: cd plugins/channel-management && go generate ./...
//
// 修改后端 service 包的枚举常量（BillingMode* / Status* / Platform* /
// BillingModelSource*）后，重新运行上述命令同步本文件。
//
// 校验路径：codegen 工具在生成前会调用 service.BillingModes() 等 List
// 函数交叉比对，缺枚举值会直接报错。

// Billing mode — 与 service.BillingMode* 对齐。
export const BILLING_MODE_TOKEN = 'token' as const
export const BILLING_MODE_PER_REQUEST = 'per_request' as const
export const BILLING_MODE_IMAGE = 'image' as const
export type BillingMode =
  | typeof BILLING_MODE_TOKEN
  | typeof BILLING_MODE_PER_REQUEST
  | typeof BILLING_MODE_IMAGE

export const BILLING_MODES: ReadonlyArray<BillingMode> = [
  BILLING_MODE_TOKEN,
  BILLING_MODE_PER_REQUEST,
  BILLING_MODE_IMAGE,
] as const

// Channel status — 与 service.StatusActive / StatusDisabled 对齐。
export const CHANNEL_STATUS_ACTIVE = 'active' as const
export const CHANNEL_STATUS_DISABLED = 'disabled' as const
export type ChannelStatus =
  | typeof CHANNEL_STATUS_ACTIVE
  | typeof CHANNEL_STATUS_DISABLED

export const CHANNEL_STATUSES: ReadonlyArray<ChannelStatus> = [
  CHANNEL_STATUS_ACTIVE,
  CHANNEL_STATUS_DISABLED,
] as const

// Platform — 与 service.Platform* 对齐。
export const PLATFORM_ANTHROPIC = 'anthropic' as const
export const PLATFORM_OPENAI = 'openai' as const
export const PLATFORM_GEMINI = 'gemini' as const
export const PLATFORM_ANTIGRAVITY = 'antigravity' as const
export type Platform =
  | typeof PLATFORM_ANTHROPIC
  | typeof PLATFORM_OPENAI
  | typeof PLATFORM_GEMINI
  | typeof PLATFORM_ANTIGRAVITY

export const PLATFORMS: ReadonlyArray<Platform> = [
  PLATFORM_ANTHROPIC,
  PLATFORM_OPENAI,
  PLATFORM_GEMINI,
  PLATFORM_ANTIGRAVITY,
] as const

// Billing model source — 与 service.BillingModelSource* 对齐。
export const BILLING_MODEL_SOURCE_REQUESTED = 'requested' as const
export const BILLING_MODEL_SOURCE_UPSTREAM = 'upstream' as const
export const BILLING_MODEL_SOURCE_CHANNEL_MAPPED = 'channel_mapped' as const
export type BillingModelSource =
  | typeof BILLING_MODEL_SOURCE_REQUESTED
  | typeof BILLING_MODEL_SOURCE_UPSTREAM
  | typeof BILLING_MODEL_SOURCE_CHANNEL_MAPPED

export const BILLING_MODEL_SOURCES: ReadonlyArray<BillingModelSource> = [
  BILLING_MODEL_SOURCE_REQUESTED,
  BILLING_MODEL_SOURCE_UPSTREAM,
  BILLING_MODEL_SOURCE_CHANNEL_MAPPED,
] as const
