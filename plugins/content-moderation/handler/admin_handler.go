// Package handler holds the admin REST handlers (config, logs, status,
// unban-user, flagged-hashes). It is migrated from the core's content
// moderation handler in a follow-up task.
package handler
