// Package service holds the core content-moderation logic (policy evaluation,
// API-key calls, keyword matching, hash cache, ban escalation). It is migrated
// from backend/internal/service/content_moderation.go in a follow-up task.
package service
