/**
 * Chinese i18n messages for gateway-antigravity plugin.
 *
 * Currently empty -- all form keys (admin.accounts.*) are defined in the
 * host's i18n and available because the form renders in the host Vue tree.
 * Add plugin-specific keys here if needed in the future.
 */
export default {}
