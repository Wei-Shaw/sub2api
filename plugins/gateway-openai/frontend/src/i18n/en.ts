/**
 * gateway-openai plugin en i18n namespace.
 *
 * Currently empty — kept as a placeholder so future keys can be added without
 * touching the install() registration. Flat-merged into host vue-i18n via
 * sdk.i18n.registerNamespace('gateway-openai', { en, zh }).
 */
export default {}
